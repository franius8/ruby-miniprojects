require './lib/cipher'

describe CipherEncode do
    before(:example) do
        @shift = 2
        @string = 'a'
      end
    describe '#shift_value' do
        it 'shifts the letter by shift factor'
        encode = CipherEncode.new
    end

end